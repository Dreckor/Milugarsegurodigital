<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.escole.milugarsegurodigital">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/recurso_9mdpi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".describe"></activity>
        <activity android:name=".pick_your_emotion" />
        <activity android:name=".how_do_you_feel" />
        <activity android:name=".breath" />
        <activity android:name=".select_activity" />
        <activity android:name=".before_start" />
        <activity android:name=".nickname_pick" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>